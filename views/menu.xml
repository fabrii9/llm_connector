<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="action_llm_provider" model="ir.actions.act_window">
        <field name="name">Proveedores LLM</field>
        <field name="res_model">llm.provider</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_llm_provider_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Configurá tu primer proveedor LLM
            </p>
            <p>
                Elegí el proveedor (Kimi, OpenAI, Anthropic, Gemini, Groq),
                pegá la API key y probá la conexión. Otros módulos podrán
                usar esta conexión vía llm.provider.
            </p>
        </field>
    </record>

    <menuitem
        id="menu_llm_connector_root"
        name="LLM Connector"
        sequence="50"
        groups="group_llm_manager"
    />
    <menuitem
        id="menu_llm_provider"
        name="Proveedores"
        parent="menu_llm_connector_root"
        action="action_llm_provider"
        sequence="10"
    />
</odoo>
